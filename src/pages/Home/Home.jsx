// src/pages/Home/Home.jsx
// Placeholder only — gets replaced with the real Home page in Milestone 3.
// Its only job right now is to prove the deploy pipeline works end to end.

const Home = () => {
  return (
    <div className="mx-auto flex min-h-[70vh] max-w-7xl flex-col items-center justify-center px-4 text-center">
      <h1 className="font-display text-3xl text-base-content">
        🚀 Skeleton deploy successful
      </h1>
      <p className="mt-2 text-base-content/60">
        Navbar, routing, and theme are wired up. Real Home sections come next.
      </p>
    </div>
  );
};

export default Home;